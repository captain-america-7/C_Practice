#include <stdio.h>
    int main()
{
    int a = 35;
    float b = 3.24;
    printf("%d %f %d", a, b + 1.5, 235);
}
// Error is before #include<stdio.h>.we can not write like this * / at the end.
// Nested comments are not possible
